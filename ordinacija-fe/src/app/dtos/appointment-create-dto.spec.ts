import { AppointmentCreateDto } from './appointment-create-dto';

describe('AppointmentCreateDto', () => {
  it('should create an instance', () => {
    expect(new AppointmentCreateDto()).toBeTruthy();
  });
});
